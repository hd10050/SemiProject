<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mem">


	<!-- 회원 가입 -->
	<insert id="joinMem" parameterType="mybatis.vo.MemberVO">
		insert into member_table(m_idx, m_id, m_pw, m_name, m_gender, m_status, m_phone) 
		values(member_table.sesq, #{m_id}, #{m_pw}, #{m_name}, #{m_gender}, 0, #{m_phone})
	</insert>
	
	<!-- 회원 정보 수정 -->
	<update id="updateMem" parameterType="mybatis.vo.MemberVO">
		update	member_table
		<set>
			<trim suffixOverrides=",">
				<if test="m_pw != null">
					m_pw = #{m_pw},
				</if>
				<if test="m_name != null">
					m_name = #{m_name},
				</if>
				<if test="m_gender != null">
					m_gender = #{m_gender},
				</if>
				<if test="m_phone != null">
					m_phone = #{m_phone},
				</if>
			</trim>
		</set>
		where m_idx = #{m_idx} 
	</update>
	
	<!-- 회원 탈퇴 -->
	<update id="delMem" parameterType="mybatis.vo.MemberVO">
		update member_table set status = 1 where m_idx = #{m_idx} and m_pw = #{m_pw}
	</update>
	
	
		<!-- 회원 로그인 -->
	<select id="login" resultType="mybatis.vo.MemberVO" parameterType="java.util.Map">
		select * from member_table where m_id = #{m_id} and m_pw = #{m_pw} and status = 0
	</select>
	
	
	
	
	



</mapper>